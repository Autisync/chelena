// Placeholder until real types are generated against a running Supabase
// instance: `supabase gen types typescript --local > lib/supabase/types.ts`
// (see README.md "Local dev"). Using `any` here (not `unknown`) is
// deliberate — swap this file in place once migrations have been applied.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
